 .data # Program at 2.1
# ****** class Object ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 207 # 'O' with high bit set # ClassDecl at 0.0
  .byte 98 # 'b' # ClassDecl at 0.0
  .byte 106 # 'j' # ClassDecl at 0.0
  .byte 101 # 'e' # ClassDecl at 0.0
  .byte 99 # 'c' # ClassDecl at 0.0
  .byte 116 # 't' # ClassDecl at 0.0
3652:CLASS_Object: # ClassDecl at 0.0
  .word fcn_hashCode_Object # MethodDeclNonVoid at 0.0
  .word fcn_equals_Object # MethodDeclNonVoid at 0.0
  .word fcn_toString_Object # MethodDeclNonVoid at 0.0
# ****** class String ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 211 # 'S' with high bit set # ClassDecl at 0.0
  .byte 116 # 't' # ClassDecl at 0.0
  .byte 114 # 'r' # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
  .byte 103 # 'g' # ClassDecl at 0.0
3672:CLASS_String: # ClassDecl at 0.0
  .word fcn_hashCode_String # MethodDeclNonVoid at 0.0
  .word fcn_equals_String # MethodDeclNonVoid at 0.0
  .word fcn_toString_String # MethodDeclNonVoid at 0.0
  .word fcn_concat_String # MethodDeclNonVoid at 0.0
  .word fcn_substring_String # MethodDeclNonVoid at 0.0
  .word fcn_length_String # MethodDeclNonVoid at 0.0
  .word fcn_charAt_String # MethodDeclNonVoid at 0.0
  .word fcn_compareTo_String # MethodDeclNonVoid at 0.0
3704:END_CLASS_String: # ClassDecl at 0.0
# ****** class Lib ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 204 # 'L' with high bit set # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 98 # 'b' # ClassDecl at 0.0
3708:CLASS_Lib: # ClassDecl at 0.0
  .word fcn_hashCode_Object # MethodDeclNonVoid at 0.0
  .word fcn_equals_Object # MethodDeclNonVoid at 0.0
  .word fcn_toString_Object # MethodDeclNonVoid at 0.0
  .word fcn_readLine_Lib # MethodDeclNonVoid at 0.0
  .word fcn_readInt_Lib # MethodDeclNonVoid at 0.0
  .word fcn_readChar_Lib # MethodDeclNonVoid at 0.0
  .word fcn_printStr_Lib # MethodDeclVoid at 0.0
  .word fcn_printBool_Lib # MethodDeclVoid at 0.0
  .word fcn_printInt_Lib # MethodDeclVoid at 0.0
  .word fcn_intToString_Lib # MethodDeclNonVoid at 0.0
  .word fcn_intToChar_Lib # MethodDeclNonVoid at 0.0
# ****** class Main ****** # ClassDecl at 2.7
  .byte 205 # 'M' with high bit set # ClassDecl at 2.7
  .byte 97 # 'a' # ClassDecl at 2.7
  .byte 105 # 'i' # ClassDecl at 2.7
  .byte 110 # 'n' # ClassDecl at 2.7
3756:CLASS_Main: # ClassDecl at 2.7
  .word fcn_hashCode_Object # MethodDeclNonVoid at 0.0
  .word fcn_equals_Object # MethodDeclNonVoid at 0.0
  .word fcn_toString_Object # MethodDeclNonVoid at 0.0
  .word fcn_readLine_Lib # MethodDeclNonVoid at 0.0
  .word fcn_readInt_Lib # MethodDeclNonVoid at 0.0
  .word fcn_readChar_Lib # MethodDeclNonVoid at 0.0
  .word fcn_printStr_Lib # MethodDeclVoid at 0.0
  .word fcn_printBool_Lib # MethodDeclVoid at 0.0
  .word fcn_printInt_Lib # MethodDeclVoid at 0.0
  .word fcn_intToString_Lib # MethodDeclNonVoid at 0.0
  .word fcn_intToChar_Lib # MethodDeclNonVoid at 0.0
  .word fcn_main_Main # MethodDeclVoid at 3.17
3804:END_CLASS_Main: # ClassDecl at 2.7
3804:END_CLASS_Lib: # ClassDecl at 0.0
# ****** class RunMain ****** # ClassDecl at 0.0
  .byte 0 # ClassDecl at 0.0
  .byte 210 # 'R' with high bit set # ClassDecl at 0.0
  .byte 117 # 'u' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
  .byte 77 # 'M' # ClassDecl at 0.0
  .byte 97 # 'a' # ClassDecl at 0.0
  .byte 105 # 'i' # ClassDecl at 0.0
  .byte 110 # 'n' # ClassDecl at 0.0
3812:CLASS_RunMain: # ClassDecl at 0.0
  .word fcn_hashCode_Object # MethodDeclNonVoid at 0.0
  .word fcn_equals_Object # MethodDeclNonVoid at 0.0
  .word fcn_toString_Object # MethodDeclNonVoid at 0.0
3824:END_CLASS_RunMain: # ClassDecl at 0.0
3824:dataArrayVTableStart: # IntegerType at 0.0
3824:END_CLASS_Object: # ClassDecl at 0.0
  .byte 10 # StringLiteral at 7.21
  .byte 0 # StringLiteral at 7.21
  .byte 0 # StringLiteral at 7.21
  .byte 0 # StringLiteral at 7.21
  .word CLASS_String # StringLiteral at 7.21
  .word 2 # StringLiteral at 7.21
  .word -1 # StringLiteral at 7.21
3840:strLit_22: # StringLiteral at 7.21
.text
.globl main
0:main:
0#  jal vm_init
4#li $s6,2
8#li $s7,0
12#jal newObject
16#la $t0,CLASS_Main
20#sw $t0,-12($s7)
24#lw $s2,($sp)
28#addu $sp,$sp,4
32#jal fcn_main_Main
36#li $v0,10
40#syscall
44:fcn_main_Main:
44#  subu $sp,$sp,4
48#  sw $ra,($sp)
52#  li $t0,0
56#  subu $sp,$sp,8
60#  sw $s5,4($sp)
64#  sw $t0,($sp)
68#  lw $0,($sp)
72:while_cond_28:
72#  lw $t0,0($sp)
76#  subu $sp,$sp,8
80#  sw $s5,4($sp)
84#  sw $t0,($sp)
88#  li $t0,20
92#  subu $sp,$sp,8
96#  sw $s5,4($sp)
100#  sw $t0,($sp)
104#  lw $t2,($sp)
108#  addu $sp,$sp,8
112#  lw $t1,($sp)
116#  addu $sp,$sp,8
120#slt $t0,$t1,$t2
124#  subu $sp,$sp,4
128#  sw $t0,($sp)
132#  lw $t0,($sp)
136#  addu $sp,$sp,4
140#  beq $t0, $0,break_target_28
144#  lw $t0,0($sp)
148#  subu $sp,$sp,8
152#  sw $s5,4($sp)
156#  sw $t0,($sp)
160#  li $t0,12
164#  subu $sp,$sp,8
168#  sw $s5,4($sp)
172#  sw $t0,($sp)
176#  lw $t2,($sp)
180#  addu $sp,$sp,8
184#  lw $t1,($sp)
188#  addu $sp,$sp,8
192#seq $t0,$t1,$t2
193##...subu $at,$t1,$t2
197##...sltiu $t0,$at,1
200#  subu $sp,$sp,4
204#  sw $t0,($sp)
208#  lw $t0,($sp)
212#  addu $sp,$sp,4
216#  beq $t0,$0,if_else_17
220#  addu $sp,$sp,0
224#j break_target_28
228#j if_done_17
232:if_else_17:
232#  addu $sp,$sp,0
236:if_done_17:
236#  subu $sp,$sp,4
240#  sw $s2,($sp)
244#  lw $t0,4($sp)
248#  subu $sp,$sp,8
252#  sw $s5,4($sp)
256#  sw $t0,($sp)
260#  lw $t0,8($sp)
264#  sw $s2,8($sp)
268#  move $s2, $t0
272#  lw $t0, -12($s2)
276#  lw $t0,32($t0)
280#  jalr $t0
284#  addu $sp,$sp,8
288#  lw $s2,($sp)
292#  addu $sp,$sp,4
296#  subu $sp,$sp,4
300#  sw $t0,($sp)
304#  addu $sp,$sp,4
308#  subu $sp,$sp,4
312#  sw $s2,($sp)
316#  la $t0, strLit_22
320#  subu $sp,$sp,4
324#  sw $t0,($sp)
328#  lw $t0,4($sp)
332#  sw $s2,4($sp)
336#  move $s2, $t0
340#  lw $t0, -12($s2)
344#  lw $t0,24($t0)
348#  jalr $t0
352#  addu $sp,$sp,4
356#  lw $s2,($sp)
360#  addu $sp,$sp,4
364#  subu $sp,$sp,4
368#  sw $t0,($sp)
372#  addu $sp,$sp,4
376#  addu $sp,$sp,0
380#  lw $t0,0($sp)
384#  subu $sp,$sp,8
388#  sw $s5,4($sp)
392#  sw $t0,($sp)
396#  lw $t0,8($sp)
400#  subu $sp,$sp,8
404#  sw $s5,4($sp)
408#  sw $t0,($sp)
412#  li $t0,1
416#  subu $sp,$sp,8
420#  sw $s5,4($sp)
424#  sw $t0,($sp)
428#  lw $t2,($sp)
432#  addu $sp,$sp,8
436#  lw $t1,($sp)
440#  addu $sp,$sp,8
444#  addu $t0,$t1,$t2
448#  subu $sp,$sp,8
452#  sw $s5,4($sp)
456#  sw $t0,($sp)
460#  lw $t0,($sp)
464#  addu $sp,$sp,8
# IdentifierExp Assignment: Assign at 4.26; stackHeight = 20
468#  sw $t0,8($sp)
472#  addu $sp,$sp,8
476#  j while_cond_28
480:break_target_28:
480#  addu $sp,$sp,8
484#  addu $sp,$sp,0
488#  addu $sp,$sp,0
492#  lw $ra,($sp)
496#  addu $sp,$sp,4
500#  jr $ra
##############################################################
# MiniJava/UP library for MIPS/Spim -- version that assumes
#    one-word boolean on stack
# author: Steven R. Vegdahl
# date: 7-13 July 2004
# modified 12-17 March 2007
# modified 3-25 May 2007
# modified 2 May 2015
# modified 7 March 2016
# modified 11 May - 31 June 2017
# modified 2024 Steven Libby changed calling conventions
#               This is now passed in s2, 
#               and methods don't pop parameters
# status: Seems to work on all test cases.
###############################################################




###############################################################
# hashCode() - library method (class Object)
# - returns a hash code for an object
# - produces int that is the object's hash code
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer (for Object)
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
504:fcn_hashCode_Object:
    # for now, we'll return the -8 offset word, shifted/added to
    # itself, then XORed with a fixed "random" value
504#    lw $t0,-8($s2) # get word with hash bits from this
508#    srl $t1,$t0,11 # shift/add to introduce more "randomness"
512#    addu $t0,$t1

516#    jr $ra # return, no change to the stack

###############################################################
# equals() - library method (class Object)
# - tests whether two objects are equal
# - produces boolean that tells whether two objects are equal--
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - object to compare to
#   - 4($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
520:fcn_equals_Object:
520#    move $t0,$s2 # first pointer (this)
524#    lw $t1,($sp) # second pointer
528#    seq $t0,$t0,$t1 # produce boolean telling if they are equal
529##...subu $at,$t0,$t1
533##...sltiu $t0,$at,1

536#    jr $ra # return

###############################################################
# hashCode() - library method (class String)
# - returns a hash code for an object
# - produces int that is the object's hash code
#   meaning that they are the same physical object
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - 1 if the objects were the same; 0 otherwise
###############################################################
540:fcn_hashCode_String:
540#    move $t0,$s2 # put this pointer in t0

544#    li $t2,0 # current hash value
548#    lw $t1,-4($t0) # negative of number of characters in string
552#    beq $t1,$zero,done_hashCode_String # if zero length => done
556#    addu $t0,$t1 # address of first character in string +12,
                 # not accounting for padding and header words
560#    and $t0,0xfffffffc # adjust for padding
561##...lui $at,65535
565##...ori $at,$at,0xfffc
569##...and $t0,$t0,$at
572#    subu $t1,$t0,$t1 # address at end of string +12
576:loop_hashCode_String:    
576#    sll $t4,$t2,5 # multiply by 32
580#    subu $t2,$t4,$t2 # subtract original, so that it's now multiply by 31
584#    lbu $t3,-12($t0) # current character
588#    addu $t2,$t3 # add in current character
592#    addu $t0,1 # go to next character's address
596#    bne $t0,$t1,loop_hashCode_String # loop back if more
600:done_hashCode_String:    

    # push int on stack
600#    move $t0, $t2 # put $t2 in return slot
604#    jr $ra # return

###############################################################
# toString() - library method (class Object)
# - returns a String version of the object
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - the string version of the object
# ****NOTE: this method counts on the fact that the hashCode method
# ****is in slot 0 of the vtable    
###############################################################
608:fcn_toString_Object:
    # we need to append:
    # - a string created from the object name in the negative portion of
    #   the vtable
    # - an at-sign "@"
    # - the hex version of the hashcode for the object, with no leading
    #   zeros

    # save return address. Get new this pointer
608#    subu $sp,4
612#    sw $ra,($sp)
    
    # get hash value
616#    lw $t2,-12($s2) # vtable address
620#    lw $t0,($t2) # method address
624#    jalr $t0
628#    subu $sp,8 # push result (int in $t0) on stack
632#    sw $s5,4($sp)
636#    sw $t0,($sp)

    # compute print-size of hash value
640#    li $t0, 1
644#    lw $t1, ($sp)
648#    srl $t1,$t1,4
652#    beq $t1,$zero,toString_skip_loop
656:toString_loop_top:
656#    addu $t0,1
660#    srl $t1,$t1,4
664#    bne $t1,$zero,toString_loop_top
668:toString_skip_loop:
668#    subu $sp,8
672#    sw $s5,4($sp)
676#    sw $t0,($sp)
    
    ################################################################
    # at this point, the stack looks like (from top):
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    ################################################################

    # determine the size of the "prefix string"
680#    lw $t2,-12($s2) # vtable address
684#    move $t0,$t2
688:toString_checkTagBit:
688#    subu $t0,1
692#    lb $t1,($t0)
696#    and $t1,0x80
697##...lui $at,0
701##...ori $at,$at,0x80
705##...and $t1,$t1,$at
708#    beq $t1,$zero,toString_checkTagBit
712#    subu $t1,$t2,$t0 # size of the prefix string, in bytes
716#    subu $sp,8
720#    sw $t1,($sp)
724#    sw $s5,4($sp) # push size onto stack

    # determine the header words for the string object's header
728#    move $s7,$zero # -4 offset header word
732#    lw $t4,8($sp) # bytes for hash value
736#    addu $t4,$t1 # bytes for prefix
    # add 8, to account for:
    #  - +1 for the '@' character
    #  - +4 for the vtable word
    #  - +3 to get the effect of rounding up rather than down
740#    addu $t4,8
744#    srl $s6,$t4,2 # divide by 4
    
    # allocate the object
748#    jal newObject
        
    ################################################################
    # at this point, the stack looks like (from top):
    # - new object, allocated to be our string
    # - number of characters in the prefix
    # - GC tag (5)
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    # $s7 also contains the address of the new object
    ################################################################

752#    li $t0,CLASS_String # store string vtable address into ...
756#    sw $t0,-12($s7) # ... -12 offset in object header
760#    lw $t2,-12($s2) # our object's vtable address
764#    lw $t5,4($sp) # number characters in prefix
768#    lw $t1,12($sp) # number of characters in hashcode
772#    addu $t3,$t1,$t5 # number of characters in string ...
776#    addu $t3,1 # ... including the '@' character
780#    subu $t0,$zero,$t3 # store negated length into ...
784#    sw $t0,-4($s7) # ... -4 offset header word
788#    lw $t0,-8($s7) # get -8 offset header-word
792#    bge $t0,$zero,skipMask_toString # don't strip hash code if not there
793##...slt $at,$t0,$zero
797##...beq $at,$zero,816
800#    and $t0,0xff # mask off object's hash value ...
801##...lui $at,0
805##...ori $at,$at,0xff
809##...and $t0,$t0,$at
812#    sw $t0,-8($s7) # ... and store back into -8 header-word
816:skipMask_toString:
816#    sll $t0,2 # convert to byte-offset (and shift off top bit, if there)
820#    subu $t0,$s7,$t0 # address first byte of string +8
#    addu $t1,$t0,$t3 # address of one past last byte of string +8
    
    # copy the prefix into the string
824#    subu $t4,$t2,$t5
828:toString_copyLoop:    
828#    lb $t3,($t4)
832#    and $t3,0x7f # mask off sign bit (in case first word)
833##...lui $at,0
837##...ori $at,$at,0x7f
841##...and $t3,$t3,$at
844#    sb $t3,-8($t0)
848#    addu $t4,1
852#    addu $t0,1
856#    blt $t4,$t2,toString_copyLoop
857##...slt $at,$t4,$t2
861##...bne $at,$zero,828

    # copy the '@' sign
864#    li $t3,'@'
868#    sb $t3,-8($t0)
                
    ################################################################
    # at this point, the stack looks like (from top):
    # - the new string object that we are building
    # - number of characters in the prefix
    # - GC tag (5)
    # - print-size (in bytes) of the hash value
    # - GC tag (5)
    # - hash value
    # - GC tag (5)
    # - return address
    # - old this pointer
    # - ... rest of stack
    # $t0 contains the address of next byte to store, +8
    # $t1 the number of characters in the hashcode
    # $s2 contains the object on which toString() has been called
    # $s7 also contains the address of the new object
    ################################################################

    # store the hex version of the hash value
872#    addu $t2,$t0,$t1 # address of last byte in string to store +8
876#    lw $t3,20($sp) # hash code to print
880:toString_hashCopyLoop:
880#    and $t4,$t3,0xf
884#    srl $t3,4
888#    lb $t4,hexChars($t4)
892#    sb $t4,-8($t2)
896#    subu $t2,$t2,1
900#    bgt $t2,$t0,toString_hashCopyLoop
901##...slt $at,$t0,$t2
905##...bne $at,$zero,880

    # blow away stack until return address
    # put return address in $ra, 
    # and put return string on top of stack
908#    addu $sp,28
912#    lw $ra,($sp)
916#    addu $sp,4
920#    move $t0, $s7 # return string we created
924#    jr $ra

###############################################################
# equals() - library method (class String)
# - the String version of the .equals method
# - produces false if the second object is null or is not a String ;
#   otherwise produces true iff the two strings have the same contents
# - parameters:
#   - $s2 - this-pointer
#   - ($sp) - object to compare to
#   - 4($sp) - old-this-pointer
# - return-value:
#   - ($sp) - 1 if the objects were the same; 0 otherwise
###############################################################
928:fcn_equals_String:
    
    # quick test for obvious false
928#    lw $t0,($sp) # second parameter
932#    beq $t0,$zero,goEsFalse # go return false if null
936#    lw $t1,-12($t0) # vtable pointer
940#    la $t2,CLASS_String # string vtable pointer
944#    bne $t1,$t2,goEsFalse # go return false if not a string

    # save $ra, $s2, second parameter
    # we don't need to swap $s2, 
    # because it's still in the string class.
948#    subu $sp,12
952#    sw $ra,8($sp)
956#    sw $s2,4($sp)
960#    sw $t0,($sp)
964#    jal fcn_compareTo_String
968#    lw $s2, 4($sp) #restore this pointer and pop parameters
972#    addu $sp, 8 # pop parameters

    # return value is 1 iff result is 0; else 0
976#    seq $t0,$t0,$zero
977##...subu $at,$t0,$zero
981##...sltiu $t0,$at,1

    # pop return address and return $t0
984#    lw $ra,($sp)
988#    addu $sp,4

992#    jr $ra # return
    
996:goEsFalse:
    # put 0 in $t0 and return
996#    li $t0, 0
1000#    jr $ra # return

###############################################################
# toString() - library method (class String)
# - returns a String version of the object
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - the string version of the object
###############################################################
1004:fcn_toString_String:
    #we are a string, so return this.
1004#    move $t0, $s2
1008#    jr $ra

###############################################################
# readLine() - library method (class Lib)
# - reads line from standard input
# - produces String that contains the line read, except that it
#   does not include the end-of-line character-sequence. An
#   end-of-line character-sequence is one of the following:
#   - a return character followed by a newline character
#   - a newline character not preceded by a return character
#   - a return character not followed by a newline character
#   - an end-of-file character that follows at least one data
#     character
# - returns null on end-of-file
# - parameter:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - pointer to string containing line that was read
# - anomalies:
#   - with bare "return", looks ahead one character to check for
#     newline.  This could cause non-intuitive behavior when
#     run interactively.
###############################################################
1012:fcn_readLine_Lib:
    
1012#    subu $sp,$sp,8 # allocate space for data tag, saving $ra
1016#    sw $ra, 4($sp) # save $ra
1020#    move $t1,$sp # save "original" sp

1024:  doRead:

    # read the character
1024#    jal readLogicalChar

    # if we have a 'return', read another character to check for
    # newline
1028#    subu $t2,$v0,13
1032#    bne $t2,$zero,notReturnRL
1036#    jal readLogicalChar
1040#    subu $t2,$v0,10 # check for newline
1044#    beq $t2,$zero,foundNewLine
1048#    sw $v0,lastCharRead # push back character into queue
1052#    j foundNewLine # go process the line

1056:  notReturnRL:

    # at this point, $v0 has our character

1056#    subu $t0,$v0,10
1060#    beq $t0,$zero,foundNewLine
1064#    blt $v0,$zero,foundEof
1065##...slt $at,$v0,$zero
1069##...bne $at,$zero,1084

    # we have a character, so push it onto stack
1072#    subu $sp,$sp,4
1076#    sw $v0,($sp)

    # loop back up to get next character
1080#    j doRead

1084:  foundEof:
    # if we had actually read some characters before hitting
        # the eof, go return them as if a newline had been read
1084#    bne $t1,$sp foundNewLine

    # otherwise, we got end of file without having read any
    # new characters, so return null
1088#    li $s7, 0 # return-value: null
1092#    j rlReturn # return

1096:  foundNewLine:

    # at this point, we have our newline (or end-of-file), and all space
    # on the stack above $t1 are characters to be put into the string.
    # That is therefore the number of data words to allocate (plus 1
    # more for the class-pointer)


    # set up GC tag and char-count on stack
1096#    subu $t0,$t1,$sp # number of chars we read (times 4)
1100#    srl $s6,$t0,2 # number of words on stack with chars
1104#    subu $sp,$sp,4 # push char-count ...
1108#    sw $s6,($sp) # ... onto stack
1112#    addu $t2,$t0,5 # GC tag, incl. for count-word
1116#    sw $t2,($t1) # store GC tag
    
    # allocate the appropriate Object
1120#    addu $s6,$s6,7 # 3 to round up, plus 1 for v-table pointer word
1124#    srl $s6,$s6,2  # data words in object
1128#    move $s7,$zero # object words in object
1132#    jal newObject  # allocate space
    
    # store header words
1136#    la $t0,CLASS_String
1140#    sw $t0,-12($s7)# store class tag (String) into object
1144#    lw $t2,4($sp) # char-count
1148#    sll $t1,$t2,2 # 4 times number of chars
1152#    subu $t2,$zero,$t2 # negative of char-count
1156#    sw $t2,-4($s7) # store negative char-count as header-word 2

    # set up pointers to various parts of stack and object
1160#    lw $t0,-8($s7) # data words in object, possibly must mask off hash
1164#    bge $t0,$zero,skipMask_readLine # don't strip hash code if not there
1165##...slt $at,$t0,$zero
1169##...beq $at,$zero,1188
1172#    and $t0,0xff # mask off object's hash value ...
1173##...lui $at,0
1177##...ori $at,$at,0xff
1181##...and $t0,$t0,$at
1184#    sw $t0,-8($s7) # ... and store back into -8 header-word
1188:  skipMask_readLine:

1188#    sll $t0,$t0,2 # data bytes in object
1192#    subu $t0,$s7,$t0 # place to store first character (plus 8)    
1196#    subu $t0,$t0,$t2 # place to store last character (plus 9)
1200#    addu $sp,$sp,8 # pop redundant object-pointer and count
1204#    addu $t1,$t1,$sp # first non-char spot on stack

    # at this point:
    #  $t0 points to the target-spot for the last character (plus 9)
    #  $t1 contains top spot on the stack not containing a char
    #  $sp points to the word with the last source character

    # copy the characters, popping each off the stack

1208#    beq $sp,$t1,rlReturn
1212:  charCopyLoop:
1212#    lw $t2,($sp)
1216#    sb $t2,-9($t0)
1220#    addu $sp,$sp,4
1224#    subu $t0,$t0,1
1228#    bne $sp,$t1,charCopyLoop
1232:  rlReturn:

1232#    lw $ra, 4($sp)
1236#    addu $sp, 8   # pop $ra and pointless word off stack
1240#    move $t0, $s7 # return string we allocated in $s7
1244#    jr $ra

###################################################################
# readInt() - library method (class Lib)
# - skips whitespace
# - then attempts to read a base-10 integer from standard input
# - aborts program if a valid integer is not found
# - returns the integer that is read
# - truncates on overflow
# - parameter:
#   - $s2 - this-pointer
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - value that was read
###################################################################
1248:fcn_readInt_Lib:

    # save $ra by pushing onto stack
1248#    subu $sp,$sp,4
1252#    sw $ra,($sp)

1256:  riSkipWhiteLoop:
    # read a character
1256#    jal readLogicalChar

    # if character <= 32 ascii, check for whitespace; if not
    # whitespace, abort 
1260#    subu $t0,$v0,32
1264#    bgt $t0,$zero,nonWhite
1265##...slt $at,$zero,$t0
1269##...bne $at,$zero,1312
1272#    beq $t0,$zero,riSkipWhiteLoop # start over if space
1276#    subu $t0,$v0,10
1280#    beq $t0,$zero,riSkipWhiteLoop # start over if newline
1284#    subu $t0,$v0,9
1288#    beq $t0,$zero,riSkipWhiteLoop # start over if tab
1292#    subu $t0,$v0,13
1296#    beq $t0,$zero,riSkipWhiteLoop # start over if carriage return
1300#    subu $t0,$v0,12
1304#    beq $t0,$zero,riSkipWhiteLoop # start over if form-feed
1308#    j badIntegerFormat # illegal integer char: abort program

1312:  nonWhite:
1312#    subu $t0,$v0,'-'
1316#    li $t4,1 # final multiplier
1320#    bne $t0,$zero,helpReadInt # go read
1324#    li $t4,-1 # -1 in final multiplier

    # read another character to make up for the '-'
1328#    jal readLogicalChar

1332:  helpReadInt:
1332#    li $t2,10
1336#    subu $t1,$v0,'0' # convert digit to 0-9 value
1340#    bgeu $t1,$t2,badIntegerFormat # abort if not digit
1341##...sltu $at,$t1,$t2
1345##...beq $at,$zero,3628
1348#    move $t3,$t1 #

    #### at this point, $t3 contains value of the first digit read,
    #### and $t2 contains the value 10

1352:  digitLoop:
    # loop invariants:
    # - $t3 contains the value of the number we've read so far
    # - $t2 contains the value 10

1352#    jal readLogicalChar # read next character
1356#    subu $t1,$v0,'0' # convert digit to 0-9 value
1360#    bgeu $t1,$t2,doneDigitLoop # abort if not digit
1361##...sltu $at,$t1,$t2
1365##...beq $at,$zero,1384

1368#    mul $t3,$t3,$t2 # multiply old value by 10
1369##...mult $t3,$t2
1373##...mflo $t3
1376#    addu $t3,$t3,$t1 # add in value of new digit
1380#    j digitLoop

1384:  doneDigitLoop:

    # "push back" unused character into queue
1384#    sw $v0,lastCharRead

    # restore return address
1388#    lw $ra,($sp)
1392#    addu $sp, 4

    # multiply by sign and put return value in $t0
1396#    mult $t3,$t4 # multiply to account for poss. minus sign
1400#    mflo $t3
1404#    move $t0, $t3
    
    # return
1408#    jr $ra

###################################################################
# readChar() - library method (class Lib)
# - reads a single character from standard input
# - returns the integer that is the encoding of the character
# - returns -1 if end of file was encountered
# - parameter:
#   - ($sp) - old-this-pointer
# - return-value:
#   - $t0 - value that was read
###################################################################
1412:fcn_readChar_Lib:

    # save $ra by pushing onto stack
1412#    subu $sp,$sp,4
1416#    sw $ra,($sp)

      # read the character
1420#    jal readLogicalChar

    # restore return address; put value in $t0
    # replacing this-pointer saved return address
1424#    lw $ra,($sp)
1428#    addu $sp, 4
1432#    move $t0, $v0

    # return
1436#    jr $ra

########################################################
# printStr(str) - library method (class Lib)
# - prints string to standard output
# parameters:
#   - ($sp) - the string to print
#   - 4($sp) - old-this-pointer
########################################################
1440:fcn_printStr_Lib:
    # check for null, printing "(null)", if so
1440#    lw $t0,($sp)
1444#    bne $t0,$zero,psNotNull

    # print "(null)"
1448#    la $a0,nullStr
1452#    li $v0,4
1456#    syscall
1460#    j donePrintStr
    
1464:  psNotNull:
    ##### we have a non-null string #####
    # this means that:
    # - number of data words in -8($t0):
    #   - if the sign bit is 1, the low 31 bits
    #   - if the sign bit is 0, the low 8 bits
    # - negative of number of characters in string is in -4($t0)
    # - string begins at $t0-8-(#dataWords*4), stored 1 char per byte
1464#    subu $t0,$t0,8
1468#    lw $t1,($t0) # number of data words, but needs modification
1472#    blt $t1,$zero,skipPrintStr1
1473##...slt $at,$t1,$zero
1477##...bne $at,$zero,1496
1480#    and $t1,0xff # mask out all but low byte
1481##...lui $at,0
1485##...ori $at,$at,0xff
1489##...and $t1,$t1,$at
1492#    b skipPrintStr2
1496:skipPrintStr1:
1496#    and $t1,0x7ffffff
1497##...lui $at,2047
1501##...ori $at,$at,0xffff
1505##...and $t1,$t1,$at
1508:skipPrintStr2:    
1508#    sll $t1,$t1,2
1512#    subu $t1,$t0,$t1 # first word in string
1516#    lw $t0,4($t0) # negative of string-length
1520#    subu $t0,$t1,$t0 # byte just beyond last char in string

    # print the chars in the string
1524#    beq $t0,$t1,donePrintStr
1528:  psLoop:
1528#    lb $a0,($t1) # next byte
1532#    li $v0,11 # code for printing char
1536#        syscall # print the char
1540#    addu $t1,$t1,1 # go to next char
1544#    blt $t1,$t0,psLoop
1545##...slt $at,$t1,$t0
1549##...bne $at,$zero,1528

1552:  donePrintStr:
1552#    jr $ra

########################################################
# printInt(n) - library method (class Lib)
# - prints integer in decimal format to standard output
# - parameters:
#   - ($sp) - the integer to print
#   - 4($sp) - GC tag
#   - ($sp) - old-this-pointer
########################################################
1556:fcn_printInt_Lib:
1556#    lw $a0,($sp)

    # print it
1560#    li $v0,1 # code for print-int
1564#    syscall

    # return
1568#    jr $ra

########################################################
# printBool(n) - library method (class Lib)
# - prints boolean to standard output
# - parameters:
#   - ($sp) - the boolean to print
#   - 4($sp) - old-this-pointer
########################################################
1572:fcn_printBool_Lib:
    # pop value off stack, along with 'this'
1572#    lw $t0,($sp)

    # print either 'true' or 'false', depending on the value
1576#    la $a0,falseString
1580#    beq $t0,$zero,skipPB
1584#    la $a0,trueString
1588:  skipPB:
1588#    li $v0,4 # code for print-string
1592#    syscall

    # return
1596#    jr $ra

########################################################
# str.substring(n, k) - library method (class String)
# takes a substring of a string: Java: str.substring(n, k)
# - parameters after pushing RA:
#   - ($sp) - k: one beyond index of last char in subrange
#   - 4($sp) - GC tag
#   - 8($sp) - n: index of first char in subrange
#   - 12($sp) - GC tag
#   - 16($sp) - old this pointer
# - return value:
#  - $t0 - substring
########################################################
1600:fcn_substring_String:

    # save $ra by pushing onto stack
1600#    subu $sp,$sp,4
1604#    sw $ra,($sp)

    # get string from this, we probably don't need the null check.
1608#    move $t0,$s2
1612#    beq $t0,$zero,nullPtrException

    # get both indices and string length, and ensure that
    #   0 <= n <= k <= length
1616#    lw $t0,-4($t0) # negative of string length
1620#    subu $t0,$zero,$t0 # string length
1624#    lw $t1,4($sp) # k
1628#    lw $t2,12($sp) # n
1632#    bgt $zero,$t2,strIndexOutOfBounds
1633##...slt $at,$t2,$zero
1637##...bne $at,$zero,3596
1640#    bgt $t2,$t1,strIndexOutOfBounds
1641##...slt $at,$t1,$t2
1645##...bne $at,$zero,3596
1648#    bgt $t1,$t0,strIndexOutOfBounds
1649##...slt $at,$t0,$t1
1653##...bne $at,$zero,3596

    # allocate memory
1656#    subu $s6,$t1,$t2 # # chars in target-string
1660#    addu $s6,$s6,7 # account for extra "class" (4) word + 3 to round up
1664#    srl $s6,$s6,2 # convert bytes-count to word-count
1668#    move $s7,$zero # (no object-bytes in string)
1672#    jal newObject

    # store "String" tag in object-type field
1676#    la $t0,CLASS_String
1680#    sw $t0,-12($s7)
    
    # mask off hash bits, if needed
1684#    lw $t4,-8($s7) # data words in source string
1688#    bge $t4,$zero,skipMask_substring # don't strip hash code if not there
1689##...slt $at,$t4,$zero
1693##...beq $at,$zero,1712
1696#    and $t4,0xff # mask off object's hash value ...
1697##...lui $at,0
1701##...ori $at,$at,0xff
1705##...and $t4,$t4,$at
1708#    sw $t4,-8($s7) # ... and store back into -8 header-word
1712:skipMask_substring:

    # store negative of count (=n-k) into object-length header-word
    # indicies have changed, because newObject is on stack
1712#    lw $t1,8($sp) # k
1716#    lw $t2,16($sp) # n
1720#    subu $t0,$t2,$t1 # value to store
1724#    sw $t0,-4($s7) # store value
    
1728#    move $t3,$s2 # source string pointer
    
    # skip byte-copy loop if length is zero
1732#    beq $zero,$t0,doneSubCopyzz

    # get pointers set up in preparation for copy
1736#    lw $t4,-8($t3) # data words in source string
1740#    sll $t4,$t4,2 # data bytes in source string (incl. padding)
1744#    subu $t3,$t3,$t4 # addr. of first data word of source string (+8)
1748#    addu $t3,$t3,$t2 # addr. of first source data byte to be copied (+8)
1752#    subu $t1,$t3,$t0 # addr. beyond last source data byte to be copied (+8)
1756#    lw $t2,-8($s7)   # data words in target string
1760#    sll $t2,$t2,2    # data bytes in target string
1764#    subu $t2,$s7,$t2 # addr. of first target data byte (+8)
    
    ############################################
    # at this point:
    # - we know that the string has a positive length
    # - $t3 contains 8 + address of the first source-byte
    # - $t1 contains 8 + limit-address of the first source-byte
    # - $t2 contains 8 + address first target byte
    ############################################

    # copy the bytes from source to target
1768:  subCopyLoopzz:
1768#    lb $t4,-8($t3)
1772#    sb $t4,-8($t2)
1776#    addu $t2,$t2,1
1780#    addu $t3,$t3,1
1784#    blt $t3,$t1,subCopyLoopzz
1785##...slt $at,$t3,$t1
1789##...bne $at,$zero,1768
    
1792:  doneSubCopyzz:
    # restore return address, store return value, pop stack
1792#    lw $ra,4($sp) # restore $ra
1796#    addu $sp,$sp,8 # pop ra, and allocated newObject
1800#    move $t0, $s7 # we're returning the string we got back
                  # from newObject
    
    # return
1804#    jr $ra

########################################################
# length() - library method (class String)
# returns length of a string: Java: str.length()
# - parameters:
#   - ($sp) - old this pointer
# - return-value:
#   - $t0 - length of string
########################################################
1808:fcn_length_String:

    # the length is the negative of 4($s2)
1808#    lw $t0,-4($s2) # -length
1812#    subu $t0,$zero,$t0

    # return
1816#    jr $ra


########################################################
# str1.concat(str2) - library method (class String)
# (as in Java)
# - parameters:
#   - ($sp) - the second string
#   - 4($sp) - old this pointer
#   - $s2 - the first string (this)
# - returns:
#   - $t0 - pointer to concatenated string
########################################################
1820:fcn_concat_String:
    # save $ra by pushing onto stack
1820#    subu $sp,$sp,4
1824#    sw $ra, ($sp)

    # get string pointers and check parameter for null
1828#    lw $t0,4($sp)
1832#    beq $t0,$zero,nullPtrException
1836#    move $t1,$s2
    
    # get lengths of two strings; allocate object whose size
    # is their sum divided by 4 (rounded up) plus 1
1840#    lw $t0,-4($t0) # negative size of second object
1844#    lw $t1,-4($t1) # negative size of first object
1848#    addu $s6,$t0,$t1 # sum of negative sizes
1852#    sra $s6,$s6,2 # negative word-size of char part
1856#    subu $s6,$zero,$s6 # word size of char part
1860#    addu $s6,$s6,1 # data word size, including v-table word
1864#    move $s7,$zero
1868#    jal newObject

    # store String vtable address in vtable address field
1872#    la $t0,CLASS_String
1876#    sw $t0,-12($s7)
        
    # mask off hash bits, if needed
1880#    lw $t4,-8($s7) # # data words in source string
1884#    bge $t4,$zero,skipMask_concat # don't strip hash code if not there
1885##...slt $at,$t4,$zero
1889##...beq $at,$zero,1908
1892#    and $t4,0xff # mask off object's hash value ...
1893##...lui $at,0
1897##...ori $at,$at,0xff
1901##...and $t4,$t4,$at
1904#    sw $t4,-8($s7) # ... and store back into -8 header-word
1908:skipMask_concat:

1908#    move $t9, $s7 # store return string in unused (I hope) t9 register

    #restore $t0, and $t1 after they were potentially blown
    #away by newObject
1912#    move $t1,$s2   # first object
1916#    lw $t0,8($sp) # second object
    
    # get negative sizes; sum and store in new object size-field
1920#    lw $t2,-4($t0) # negative length of second object
1924#    lw $t3,-4($t1) # negative length of first object
1928#    addu $t4,$t2,$t3 # sum of negative lengths
1932#    sw $t4,-4($s7) # store sum as negated target-string length

    #########################################################
    # at this point:
    # - $t0 is pointer to second object
    # - $t1 is pointer to first object
    # - $t2 is negated length of second object
    # - $t3 is negated length of first object
    # - $s7 is pointer to new object
    #########################################################

    # compute addresses for moving data from first string
1936#    lw $t4,-8($t1) # # data words in first string
1940#    sll $t4,$t4,2 # # data bytes in first string
1944#    subu $t1,$t1,$t4 # addr. (+8) of first byte in first string
1948#    lw $t4,-8($s7) # # data words in new string
1952#    sll $t4,$t4,2 # # data bytes in new string
1956#    subu $s7,$s7,$t4 # addr. (+8) of first byte in new string
1960#    beq $zero,$t3,doneConcatLoop1zz # skip first loop is no bytes to copy
1964#    subu $t3,$t1,$t3 # limit (+8) address for first string

    #########################################################
    # at this point:
    # - $t0 is pointer to second object
    # - $t1 is address (+8) of first byte in first object
    # - $t2 is negated length of second object
    # - $t3 is limit-address (+8) of data in first object
    # - $s7 is address (+8) of first byte in new object
    # - note: if data-length of first object is zero, then
    #   we skip over this part, and go to 'doneConcatLoop1'
    #########################################################

    # copy the bytes from first source to target
1968:  concatLoop1zz:
1968#    lb $t4,-8($t1)
1972#    sb $t4,-8($s7)
1976#    addu $s7,$s7,1
1980#    addu $t1,$t1,1
1984#    blt $t1,$t3,concatLoop1zz
1985##...slt $at,$t1,$t3
1989##...bne $at,$zero,1968
1992:  doneConcatLoop1zz:
  
    # if second string is empty, skip rest of copy
1992#    beq $zero,$t2,doneConcatLoop2zz

      # compute addresses for moving data from second string
1996#    lw $t4,-8($t0) # # data words in second string
2000#    sll $t4,$t4,2 # # data bytes in second string
2004#    subu $t1,$t0,$t4 # addr. (+8) of first byte in second string
2008#    subu $t3,$t1,$t2 # limit (+8) address for second string

    #########################################################
    # at this point:
    # - $t1 is address (+8) of first byte in second object
    # - $t3 is limit-address (+8) of data in second object
    # - $s7 is address (+8) of next byte to write new object
    # - note: if data-length of second object is zero, then
    #   we skip over this part, and go to 'doneConcatLoop2'
    #########################################################

    # copy the bytes from first source to target
2012:  concatLoop2zz:
2012#    lb $t4,-8($t1)
2016#    sb $t4,-8($s7)
2020#    addu $s7,$s7,1
2024#    addu $t1,$t1,1
2028#    blt $t1,$t3,concatLoop2zz
2029##...slt $at,$t1,$t3
2033##...bne $at,$zero,2012
2036:  doneConcatLoop2zz:
 
2036:  concatRtnzz:
    # return
2036#    move $t0, $t9 # put saved return string in $t0
2040#    lw $ra, 4($sp)
2044#    addu $sp, 8 # pop ra and allocated newObject
2048#    jr $ra

########################################################
# str.charAt(n) - library method (class String)
# accesses a character in a string, as in Java
# - parameters:
#   - ($sp) - the index, n
#   - 4($sp) - GC tag
#   - 8($sp) - old this pointer
#   - $s2    - this pointer
# - returns:
#   - $t0 - the character found.
########################################################
2052:fcn_charAt_String:
    # get string
2052#    move $t0,$s2

    # check that index is in bounds
2056#    lw $t1,-4($t0) # negative of # data words in string
2060#    subu $t3,$zero,$t1 # # chars in string
2064#    lw $t2,($sp) # index
2068#    bgeu $t2,$t3,strIndexOutOfBounds
2069##...sltu $at,$t2,$t3
2073##...beq $at,$zero,3596

    # access element
2076#    lw $t1,-8($t0) # # data words in object
2080#    sll $t1,$t1,2 # - byte-offset from end of chars
2084#    subu $t1,$t2,$t1 # - address of first char in string, offset by 8
2088#    addu $t0,$t0,$t1 # - address of our char, offset by 8
2092#    lb $t0,-8($t0) # our char

    # return
2096#    jr $ra

########################################################
# intToString(n) - library method (class Lib)
# converts int to string: Java: ""+n
# - parameters:
#   - ($sp) - the value to convert, n
#   - 4($sp) - GC tag
#   - 8($sp) - old-this-pointer
#   - $s2 - this-pointer
# - returns:
#   - $t0 - the string, which is the string representation of
#     the integer
########################################################
2100:fcn_intToString_Lib:

    # save return address on stack; allocate space for dummy GC tag
2100#    subu $sp,$sp,8
2104#    sw $ra,4($sp)

    # save current sp
2108#    move $t0,$sp

    # move constant 10 into $t3 
    # (Good, I didn't know what li did, I'm glad this comment was here)
    # * slams head through wall *
2112#    li $t3,10

    # get argument, negate if negative
2116#    lw $t1,8($sp)
2120#    bge $t1,$zero,itsNonNegLoop
2121##...slt $at,$t1,$zero
2125##...beq $at,$zero,2132
2128#    subu $t1,$zero,$t1

    # loop through, computing unsigned remainder by 10, and
    # storing digits on stack until we reach 0
2132:  itsNonNegLoop:
2132#    divu $t1,$t3
2136#    mflo $t1 # quotient
2140#    mfhi $t4 # remainder
2144#    addu $t4,$t4,'0' # turn remainder into digit
2148#    subu $sp,$sp,4
2152#    sw $t4,($sp) # push digit onto stack
2156#    bne $t1,$zero,itsNonNegLoop

    # push '-' if negative
2160#    lw $t4,8($t0)
2164#    bge $t4,$zero,itsSkipNeg
2165##...slt $at,$t4,$zero
2169##...beq $at,$zero,2184
2172#    li $t4,'-'
2176#    subu $sp,$sp,4
2180#    sw $t4,($sp)
2184:  itsSkipNeg:

    ################################################
    # At this point, all of our digits have been pushed
    # onto the stack.  $sp points to the first one;
    # $t0 contains the limit-pointer (into which we need to
    # write a GC tag).
    ################################################

    # compute number of characters on stack (one word per character);
    # write GC tag onto stack; push char-count onto stack
2184#    subu $s6,$t0,$sp
2188#    addu $t3,$s6,5 # GC tag (including for count-word, about to be pushed)
2192#    sw $t3,($t0)
2196#    srl $s6,$s6,2
2200#    subu $sp,$sp,4
2204#    sw $s6,($sp)
    
    # allocate memory
2208#    addu $s6,$s6,7 # 3 to round up, plus 4 for vtable word
2212#    srl $s6,$s6,2
2216#    move $s7,$zero # no "object" words in object
2220#    jal newObject

    # restore char-count; pop it and return value from 'newObject'
2224#    lw $t0,4($sp)
2228#    addu $sp,$sp,8

    # store "String" tag into class field
2232#    subu $s7,$s7,8 # address of header-1 word
2236#    la $t1,CLASS_String
2240#    sw $t1,-4($s7)
        
    # mask off hash bits, if needed
2244#    lw $t4,($s7) # # data words in source string
2248#    bge $t4,$zero,skipMask_intToString # don't strip hash code if not there
2249##...slt $at,$t4,$zero
2253##...beq $at,$zero,2272
2256#    and $t4,0xff # mask off object's hash value ...
2257##...lui $at,0
2261##...ori $at,$at,0xff
2265##...and $t4,$t4,$at
2268#    sw $t4,($s7) # ... and store back into -8 header-word
2272:skipMask_intToString:
    
    # store negative of char-count into header-2 word
2272#    subu $t0,$zero,$t0
2276#    sw $t0,4($s7)

2280#    lw $t1,($s7) # number of data words in string
2284#    sll $t1,$t1,2 # number data bytes in string
2288#    subu $t1,$s7,$t1 # first location to store chars in string
2292#    subu $t0,$t1,$t0 # limit address for chars in string

    ####################################################
    # at this point:
    # - $sp contains first source character address
    # - $t1 contains first target character address
    # - $t0 contains target-limit address
    ####################################################

    # loop through and copy all elements as we pop them off the stack.
    # (In this case, we know that there is it least one.)
2296:  itsLoop:
2296#    lw $t2,($sp)
2300#    addu $sp,$sp,4
2304#    sb $t2,($t1)
2308#    addu $t1,$t1,1
2312#    bne $t1,$t0,itsLoop

    ####################################################
    # At this point
    # - ($t0+15)&0xfffffffe is our return value
    # - ($sp) contains garbage (old GC tag)
    # - 4($sp) contains return address
    ####################################################

    # adjust stack, restore return address; return
2316#    lw $ra,4($sp)
2320#    addu $sp,$sp,8
2324#    addu $t0,$t0,15
2328#    and $t0,$t0,0xfffffffc
2329##...lui $at,65535
2333##...ori $at,$at,0xfffc
2337##...and $t0,$t0,$at
2340#    jr $ra

########################################################
# intToChar(n) - library method (class Lib)
# converts int to a one-character string: Java: ""+(char)(n&0xff)
# - parameters:
#   - ($sp) - the value to convert, n
#   - 4($sp) - GC tag
#   - 8($sp) - old-this-pointer
# - returns:
#   - $t0 - the string, which is the converted character
# - note: only the low 8 bits of the value n are used
########################################################
2344:fcn_intToChar_Lib:
    # save return address
2344#    subu $sp,$sp,4
2348#    sw $ra,($sp)

    # allocate object
2352#    li $s6,2
2356#    move $s7,$zero
2360#    jal newObject

    # get n from string.
2364#    lw $t1,8($sp)

    # store "String" tag into class field
2368#    la $t0,CLASS_String
2372#    sw $t0,-12($s7)
        
    # mask off hash bits, if needed
2376#    lw $t4,-8($s7) # # data words in source string
2380#    bge $t4,$zero,skipMask_intToChar # don't strip hash code if not there
2381##...slt $at,$t4,$zero
2385##...beq $at,$zero,2404
2388#    and $t4,0xff # mask off object's hash value ...
2389##...lui $at,0
2393##...ori $at,$at,0xff
2397##...and $t4,$t4,$at
2400#    sw $t4,-8($s7) # ... and store back into -8 header-word
2404:  skipMask_intToChar:

    # store data in string
2404#    sb $t1,-16($s7)
    
    # store negative of size in header-word 2
2408#    li $t0,-1
2412#    sw $t0,-4($s7)

    # we're returning $s7
2416#    move $t0, $s7

    # restore $ra, and pop return address and return string off stack
2420#    lw $ra,4($sp)
2424#    addu $sp,$sp,8

    # return
2428#    jr $ra

########################################################
# str1.compareTo(str2) - library method (class String)
# compares two strings as in Java
# - parameters:
#   - ($sp) - second string
#   - 4($sp) - old this pointer
#   - $s2 - first string (this)
# - returns:
#   - $t0 - -1, 0, or 1 depending on whether str1 is
#       lexographically less than, equal to or greater than str2
########################################################
2432:fcn_compareTo_String:
    # get string pointers and check parameter for null
2432#    lw $t0,($sp) # second string
2436#    beq $t0,$zero,nullPtrException
2440#    move $t1,$s2 # first string
    
    # get (negatives of) respective byte-lengths
2444#    lw $t2,-4($t0) # negative length of second string
2448#    lw $t3,-4($t1) # negative length of first string

    # put tentative return value in $t5.
    # The tentative return value is the one that we will use if we get
    # to the end of the shorter string during our comparison-loop.
2452#    slt $t4,$t3,$t2
2456#    slt $t5,$t2,$t3
2460#    subu $t5,$t5,$t4
    
    # at this point:
    # - $t0 contains the pointer to the second string object
    # - $t1 contains the pointer to the first string object
    # - $t5 contains the value to return if the strings compare equal up
    #   to the length of the shortest word

    # get begin-of-string addresses
2464#    lw $t2,-8($t0) # # data words in second string
2468#    lw $t3,-8($t1) # # data words in first string
2472#    sll $t2,$t2,2 # byte-offset to beginning of str2 (+8)
2476#    sll $t3,$t3,2 # byte-offset to beginning of str1 (+8)
2480#    subu $t0,$t0,$t2 # beginning of str1 address (+8)
2484#    subu $t1,$t1,$t3 # beginning of str2 address (+8)
    
    # put $t1-limit into $t2
2488#    beq $zero,$t5,skipStrCmpLenzz
2492#    move $t2,$t3
2496:  skipStrCmpLenzz:
2496#    add $t2,$t1,$t2
    
    # at this point:
    # - $t0 contains 8 plus the address of the first data-byte of str2
    # - $t1 contains 8 plus the address of the first data-byte of str1
    # - $t2 contains 8 plus the address of the last data-type of str1
    # - $t5 contains the value to return if the strings compare equal up
    #   to the length of the shortest word

    #######################################################
    # at this point, we have
    # - $t5 containing the tentative return-value
    # - $t1 containing address of first char in str2
    # - $t2 containing limit for $t1
    # - $t0 containing address of first char in str1
    #######################################################

    # loop through, until we find unequal words or we hit
    # our limit    
2500:  cmpLoopzz:
2500#    lw $t3,-8($t1) # word from str2
2504#    lw $t4,-8($t0) # word from str1
2508#    bne $t3,$t4,cmpNotEqualzz
2512#    addu $t1,$t1,4
2516#    addu $t0,$t0,4
2520#    bne $t1,$t2,cmpLoopzz

    # # got to the end of one string: go set up return
2524#    j cmpHitLimitzz

2528:  cmpNotEqualzz:
    # found unequal characters: return -1 or 1, depending on which is
    # greater
2528#    slt $t5,$t4,$t3 # 1 if str2 > str1, else 0
2532#    sll $t5,$t5,1 # 2 if str2 > str1, else 0
2536#    subu $t5,$t5,1 # 1 if str2 > str1, else -1
2540:  cmpHitLimitzz:
2540#    move $t0, $t5 # store return value
2544#    jr $ra # return

########################################################
# readLogicalChar (millicode)
#  - logically reads a character from standard input
#  - first checks character buffer, consuming it if possible
#  - return-result:
#    - returns character in $v0
#  - side-effects:
#    - reads a character
#    - clobbers $t0
########################################################
2548:readLogicalChar:

    # check if we already have a character
2548#    lw $v0,lastCharRead
2552#    li $t0,-2
2556#    beq $t0,$v0 doReadCh

    # we have our character from the buffer. Wipe out
    # buffer and return
2560#    sw $t0,lastCharRead # store -2 into buffer (i.e. "empty")
2564#    jr $ra # return
    
2568:  doReadCh:
    # we need to actually read a character: read and return
2568#    li $v0,12 # use system call to read a character
2572#    syscall
2576#    jr $ra # return

########################################################
# newObject (millicode)
#  - allocates a new object on the heap
#  - two-word header is set up properly
#  - all non-header words in object are set to zero
#  - parameters:
#    - $s6 = the number of data words in the negative-offset portion of
#      the object (including the vtable pointer); this value is 1 if the
#      object is an array; this should always be at least 1
#    - $s7 - second header-word, which is the number of object
#      words in the object
#      - $s7 may not be negative (this is checked); if so, the program is
#        aborted with an "array size of out bounds" error message
#      is treated as if it were zero
#  - return-result:
#    - returned in $s7
#    - and pushed on the stack
#  - side-effects:
#  - may trash all $tx registers and all $sx registers, except the
#    "permanent" ones, which get updated with values that are
#    consistent with the new space
########################################################
2580:newObject:
    # $s6 = # data words
    # $s7 = # object words (# data words if data-array allocation,
    # where a negative number is treated as 0)
    # $ra = return address
    # on return, pointer to new memory in two places:
    # - pushed onto top of stack
    # - in $s7
    
    # if the object-portion size is negative, abort with error
2580#    blt $s7,$zero arraySizeOutOfBounds
2581##...slt $at,$s7,$zero
2585##...bne $at,$zero,3612

    # mark the fact that we have not yet GC'd for this allocation
2588#    move $t5,$zero

    ###### TEMPORARY #######
    # for now, go a GC unconditionally, so that a full GC occurs
    # every time we allocate an object
    ### let's not do that for now
    # j doGC

2592:  gcJoin:

    # at this point:
    #  $s6 contains the before-header word count
    #  $s7 contains the second header word, which is also the
    #    after-header word count
    #  $t5 is zero iff we have not GC'd for this allocation

    # determine if we have enough memory available
2592#    addu $t0,$s6,$s7
2596#    sll $t0,$t0,2
2600#    addu $t0,$t0,8 # amount of memory needed
2604#    addu $t1,$t0,$s3
2608#    bgtu $t1,$s4,doGC
2609##...sltu $at,$s4,$t1
2613##...bne $at,$zero,2736

    # at this point:
    #  $s3 points to beginning of segment, and
    #  $t1 points just past the end

    # zero out the memory
2616#    move $t2,$s3
2620:  zeroObjLoop:
2620#    sw $zero,($s3)
2624#    lw $zero,($s3)

2628#    addu $s3,4
2632#    bltu $s3,$t1,zeroObjLoop
2633##...sltu $at,$s3,$t1
2637##...bne $at,$zero,2620

    # at this point:
    #  $s3 has been updated to point to the next free slot,
    #    which is also the points just past our object

    # compute pointer value and set up header-words
2640#    sll $t0,$s7,2    # number of post-header bytes
2644#    subu $t0,$s3,$t0 # pointer that we will return

    # determine the value to store in the first header-word:
    # if <= 255 (unsigned):
    #  - it goes into low byte
    #  - hash bits go into high three bytes, with top bit being 1
    # if >255 (unsigned)
    #  - it goes into full word (with a 0 in the high bit)
    
2648#    li $t2,255
2652#    bgeu $s6,$t2 firstHeaderLarge
2653##...sltu $at,$s6,$t2
2657##...beq $at,$zero,2704
2660#    lw $t1,prevHashConstant
2664#    addu $t1,0x12b68fd2
2665##...lui $at,4791
2669##...addiu $at,$at,-28718
2673##...addu $t1,$at,$t1
2676#    sw $t1,prevHashConstant # update hash constant
2680#    or $t1,0x80000000 # set top bit
2681##...lui $at,32768
2685##...or $t1,$at,$t1
2688#    and $t1,0xffffff00 # clear low bit
2689##...lui $at,65535
2693##...ori $at,$at,0xff00
2697##...and $t1,$t1,$at
2700#    or $s6,$t1
2704:firstHeaderLarge:
2704#    sw $s6,-8($t0) # store header-1 value
2708#    lw $zero,-8($t0)

    # store second header-value
2712#    sw $s7,-4($t0)   # second header-word
2716#    lw $zero,-4($t0)
    
    # put return-value into $s7 and also push it onto top of stack
2720#    move $s7,$t0
2724#    subu $sp,$sp,4
2728#    sw $t0,($sp)

2732#    jr $ra
    
2736:  doGC:
    #####################################################
    # We need to do a garbage-collect
    #####################################################
 
     ####### print that we are doing a GC ########
     ####### THESE THREE LINES WILL NORMALLY BE COMMENTED OUT #######
#         la $a0,gcMsg # prints message: "GC!" 
#         li $v0,4 # syscall-code for print-string 
#         syscall 
       

    # if we've already done a GC for this allocation, then
    # we are actually out heap-memory: abort program
2736#    bne $t5,$zero,outOfHeapMemory
      
    # increment the number of times we've done a GC, for later reporting
2740#    lw $t5,gcCount
2744#    addu $t5,1
2748#    sw $t5,gcCount

    # save $s2 (our only rooted register) on the stack
    # so that the garbage collector processes it
2752#    subu $sp,$sp,4
2756#    sw $s2,($sp)

    # set $s3 to the address of the new segment and the
    # end-limit of the new segment, respectively,
    # Also, update cur_seg to refer to the other segment
2760#    lw $t0,cur_seg
2764#    move $t7,$s4
2768#    la $t6,seg1_start
2769##...lui $t6,16
2773##...addiu $t6,$t6,4212
2776#    la $s3,seg0_start
2780#    la $s4,seg0_end
2781##...lui $s4,16
2785##...addiu $s4,$s4,4212
2788#    sw $zero,cur_seg
2792#    bne $t0,$zero,skipGc1
2796#    la $t6,seg0_start
2800#    la $s3,seg1_start
2801##...lui $s3,16
2805##...addiu $s3,$s3,4212
2808#    la $s4,seg1_end
2809##...lui $s4,32
2813##...addiu $s4,$s4,4212
2816#    sw $s5,cur_seg
2820:  skipGc1:
2820#    li $t5,0x80000000
2824#    lw $t0,stack_bottom
2828#    subu $t0,4


    ###############################################################
    # at this point:
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first address of the target space
    # - $s4 contains the limit address of the target space
    # - cur_seg has been updated to refer to the target space
    # - $t0 contains the address of the deepest stack element
    # - $sp contains the address of the top stack element
    # - $s2 is available for use, having been pushed onto the stack
    # - $t5 contains the value 0x80000000
    ###############################################################

    ###### begin outer loop to copy all stack-direct objects ######

2832:  gcStackCopyLoop:
2832#    lw $t1,($t0) # current stack element

    # test if we have a GC tag
2836#    sll $t2,$t1,31 # zero iff low bit was zero
2840#    bne $t2,$zero,gcBump # go skip data values if low bit not zero

    # bump stack-address pointer
2844#    subu $t0,$t0,4

    # if value is out of range (which includes null=0), and is
    # therefore does not refer to an object on the heap, just go
    # loop back and do the next one
2848#    bleu $t1,$t6,gcTestIterDone1
2849##...sltu $at,$t6,$t1
2853##...beq $at,$zero,3016
2856#    bgtu $t1,$t7,gcTestIterDone1
2857##...sltu $at,$t7,$t1
2861##...bne $at,$zero,3016

    # if the object has already been moved, update the stack-value
    # via the forwarding pointer
2864#    lw $t2,-8($t1) # possible forwarding tag
2868#    bne $t2,$zero,gcNoForward1 # if not forwarding tag, go copy
2872#    lw $t2,-4($t1) # forwarding pointer: object's new address
2876#    sw $t2,4($t0) # update stack value
2880#    j gcTestIterDone1 # do next iteration

2884:  gcNoForward1:
    #########################################################
    # we actually need to copy the object into the new space
    #########################################################

    # compute the amount of space that is needed
2884#    bge $t2,$zero,gcSkip2
2885##...slt $at,$t2,$zero
2889##...beq $at,$zero,2904
2892#    and $t2,0xff #### mask off hash value
2893##...lui $at,0
2897##...ori $at,$at,0xff
2901##...and $t2,$t2,$at
    #### OLD
    #### subu $t2,$zero,$t2 # set to 1 if -1 (number of data words)

2904:  gcSkip2:
2904#    sll $t2,$t2,2
2908#    addu $t2,$t2,8
2912#    subu $t4,$t1,$t2 # address of first word of source
2916#    lw $t3,-4($t1) # number of object words (negative means zero)
2920#    bge $t3,$zero,gcH2Neg1
2921##...slt $at,$t3,$zero
2925##...beq $at,$zero,2932
2928#    move $t3,$zero
2932:  gcH2Neg1:
2932#    sll $t3,$t3,2
2936#    addu $t3,$t3,$t1 # address one word past last word of source

2940#    addu $t2,$s3,$t2 # pointer to target object

    #########################################################
    # At this point:
    # - $t0 contains the address of the stack slot we'll
    #   process next
    # - $t1 contains the contents of the stack slot we're
    #   currently working on, which is a pointer to the source
    #   object (i.e., the address just beyond the object's header)
    # - $t2 contains the pointer to the target object
    # - $t3 contains the limit address of the source object
    # - $t4 contains the first address of the source object
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space, which is also the first address of the
    #   target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # - $sp contains the address of the top stack element
    # - available for use: $s0-2
    #########################################################

    # swap first header word and first data word so that header
        # can be found by "trailing finger" in the target space
2944#    lw $s0,($t4)
2948#    lw $s1,-8($t1)
2952#    sw $s1,($t4)
2956#    sw $s0,-8($t1)
    
    # copy all source bytes to the target
2960:  gCinnerCopy1:
2960#    lw $s0,($t4)
2964#    sw $s0,($s3)
2968#    addu $t4,$t4,4
2972#    addu $s3,$s3,4
2976#    bltu $t4,$t3,gCinnerCopy1
2977##...sltu $at,$t4,$t3
2981##...bne $at,$zero,2960

    ###########################################################
    # All bytes have been copied to the target space.  We still
    # need to:
    # - set up forwarding pointer in source object
    # - update the pointer in the current stack slot
    ###########################################################

    # set up the forwarding pointer
2984#    sw $zero,-8($t1) # 0 in first header-slot
2988#    sw $t2,-4($t1) # forwarding pointer in second header-slot

    # update the stack slot with the address in the target space
2992#    sw $t2,4($t0)
2996#    lw $zero,-8($t2)
3000#    lw $zero,-4($t2)

    # go handle next stack slot (testing if done)
3004#    j gcTestIterDone1

3008:  gcBump:
    #### we have a GC tag.  Bytes to skip: tag value + 3.
3008#    subu $t0,$t0,$t1
3012#    subu $t0,$t0,3

3016:  gcTestIterDone1:
3016#    bgeu $t0,$sp,gcStackCopyLoop
3017##...sltu $at,$t0,$sp
3021##...beq $at,$zero,2832

    ###### end outer loop to copy all stack-direct objects ######

    #############################################################
    # We have finished processing the stack elements.  Now we need
    # to update elements in the heap itself. This may itself involve
    # moving additional objects
    #############################################################

    #########################################################
    # At this point:
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # We want to set things up so that in addition:
    # - $t0 is the "trailing finger", containing the address
    #   of the first slot in target space that we have yet
    #   to process.
    # Then during processing:
    # - $t1 will contain the contents of the heap slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # And when we're actually copying an object:
    # - $t2 will contain pointer to the target object
    # - $t3 will contain the limit address of the source object
    # - $t4 will contain the first address of the source object
    # - $s1 will contain the the limit address for the current
    #   object that tells where the pointers in the object end
    # - available for use: $s0
    ##########################################################

    # set $t0 to be at the beginning of target-space
3024#    lw $t1,cur_seg
3028#    la $t0,seg0_start
3032#    beq $t1,$zero,gcSkip4
3036#    la $t0,seg1_start
3037##...lui $t0,16
3041##...addiu $t0,$t0,4212


3044:  gcSkip4:

    # if there were no objects put into the heap during stack
    # processing, we're done, so go finish up
3044#    bgeu $t0,$s3,gcFinishUp
3045##...sltu $at,$t0,$s3
3049##...beq $at,$zero,3340

    ###### begin outer loop to copy all non-stack-direct objects ######

3052:  gcHeapCopyLoop:
    # get data count for object; swap header-word with first word
    # of object so that they're back in the right place
3052#    lw $t1,($t0) # (what will be) first header word for current object
3056#    move $t2,$t1 # number data bytes in object (poss. w/hash bits)
3060#    bge $t2,$zero,gcSkip4a # are there hash bits?
3061##...slt $at,$t2,$zero
3065##...beq $at,$zero,3080
3068#    and $t2,0xff # mask off hash bits
3069##...lui $at,0
3073##...ori $at,$at,0xff
3077##...and $t2,$t2,$at
3080:gcSkip4a:    
3080#    sll $t2,$t2,2 # # data bytes
3084#    addu $t2,$t2,8 # to skip header words
3088#    addu $t2,$t0,$t2 # pointer to new object
3092#    lw $t3,-8($t2) # word to swap
3096#    sw $t1,-8($t2) # store header word
3100#    sw $t3,($t0) # restore first word of object

    ################################################
    # at this point:
    # - $t2 contains the pointer to our new object
    ################################################

    # check if we have a data array
3104#    lw $t1,-12($t2) # vtable pointer
3108#    li $t0,dataArrayVTableStart # address of vtable for first data array
3112#    blt $t1,$t0,gcNotDataArray # if data array, no more xforming to do
3113##...slt $at,$t1,$t0
3117##...bne $at,$zero,3136

    # We have a vtable address that is a data array.
    # we need skip over it then loop back to do next object.
3120:  skipToNextObj:
3120#    lw $t1,-4($t2) # data words in (object position of) array
    #### note: we know that it's not a string, so the value will
    #### not be artificially negative
3124#    sll $t1,$t1,2 # convert to byte-count
3128#    addu $t0,$t2,$t1 # skip over object
3132#    j gcTestIterDone2 # go do next object, if any

    ###################################################
    # at this point:
    # - the object has been restored to normal status--that is, the
    #   word-swap has been done
    # - $t2 contains a pointer to the object
    ###################################################
    
3136:  gcNotDataArray:
3136#    lw $t3,-4($t2) # # object words (negative treated as zero)
3140#    bge $t3,$zero,gcH2Neg2
3141##...slt $at,$t3,$zero
3145##...beq $at,$zero,3152
3148#    move $t3,$zero
3152:  gcH2Neg2:
3152#    sll $t3,$t3,2 # # object bytes
3156#    move $t0,$t2 # address of first pointer in object
3160#    add $s1,$t2,$t3 # limit address for this object

    #####################################################
    # At this point, we have to "translate" all pointers,
    # starting at $t0 to (but not including) $s1
    #####################################################

    # if there are no pointer-slots (i.e., $t0=$s1), skip this
    # part
3164#    beq $t0,$s1,gcTestIterDone2
    
3168:  gcNextPointerInObject:
    # get pointer from object
3168#    lw $t1,($t0)

    # if value is out of range, and is therefore does not refer
    # to an object, just go loop back and do the next one
3172#    bleu $t1,$t6,gcGoNextPointerSlot
3173##...sltu $at,$t6,$t1
3177##...beq $at,$zero,3320
3180#    bgtu $t1,$t7,gcGoNextPointerSlot
3181##...sltu $at,$t7,$t1
3185##...bne $at,$zero,3320

    # if the object has already been moved, update the stack-value
    # via the forwarding pointer
3188#    lw $t8,-8($t1) # possible forwarding tag
3192#    bne $t8,$zero,gcNoForward2 # if not forwarding tag, go copy
3196#    lw $t8,-4($t1) # forwarding pointer: object's new address
3200#    sw $t8,($t0) # update pointer in object
3204#    j gcGoNextPointerSlot # do next iteration

3208:  gcNoForward2:
    #########################################################
    # we actually need to copy the object into the new space
    #########################################################

    #########################################################
    # At this point:
    # - $t0 contains the address of the heap-slot we're translating
    # - $t1 will contain the contents of the heap-slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # - $t2 will contains pointer to the object we're in the
    #   middle of translating
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $s3 contains the first unallocated address of the
    #   target space, which will also be the first address
    #   of the target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # Then during processing:
    # And when we're actually copying an object:
    # - $t3 will contain the limit address of the source object
    # - $t4 will contain the first address of the source object
    # - $s1 will contain the the limit address for the current
    #   object that tells where the pointers in the object end
    # - $t8 will contain a pointer to the target object
    # - available for use: $s0, $t8, $t9
    ##########################################################

    # compute the amount of space that is needed
3208#    bge $t8,$zero,gcSkip5 # # words in negative-offset portion
3209##...slt $at,$t8,$zero
3213##...beq $at,$zero,3228
3216#    and $t8,0xff # mask off hash bits
3217##...lui $at,0
3221##...ori $at,$at,0xff
3225##...and $t8,$t8,$at

3228:  gcSkip5:
3228#    sll $t8,$t8,2 # bytes in negative-offset portion ...
3232#    addu $t8,$t8,8 # ... add in bytes for header-words
3236#    subu $t4,$t1,$t8 # address of first word of source object
3240#    lw $t3,-4($t1) # # words in positive-offset portion
3244#    bge $t3,$zero,gcNoNeg
3245##...slt $at,$t3,$zero
3249##...beq $at,$zero,3256
3252#    move $t3,$zero # negative treated as zero
3256:  gcNoNeg:
3256#    sll $t3,$t3,2 # bytes in negative-offset portion
3260#    addu $t3,$t3,$t1 # address one word past last word of source

3264#    addu $t8,$s3,$t8 # pointer to target object

    #########################################################
    # At this point:
    # - $t0 contains the address of the stack slot we'll
    #   process next
    # - $t1 contains the contents of the stack slot we're
    #   currently working on, which is a pointer to the source
    #   object
    # - $t2 will contains pointer to the object we're in the
    #   middle of translating
    # - $t3 contains the limit address of the source object
    # - $t4 contains the first address of the source object
    # - $t5 contains the value 0x80000000
    # - $t6 contains the first address of the source space
    # - $t7 contains the limit address of the source space
    # - $t8 contains the pointer to the target object
    # - $s1 contains the the limit address for the current
    #   object that tells where the pointers in the object end
    # - $s3 contains the first unallocated address of the
    #   target space, which is also the first address of the
    #   target object
    # - $s4 contains the limit address of the target space
    # - $s5 contains the value 5
    # - $s6-7 contain the original parameters to the call to
    #   'newObject'
    # - $sp contains the address of the top stack element
    # - available for use: $s0, $t9
    #########################################################

    # swap first header word and first data word so that header
        # can be found by "trailing finger"
3268#    lw $s0,($t4)
3272#    lw $t9,-8($t1)
3276#    sw $t9,($t4)
3280#    sw $s0,-8($t1)

    # copy all source bytes to the target
3284:  gCinnerCopy2:
3284#    lw $s0,($t4)
3288#    sw $s0,($s3)
3292#    addu $t4,$t4,4
3296#    addu $s3,$s3,4
3300#    bltu $t4,$t3,gCinnerCopy2
3301##...sltu $at,$t4,$t3
3305##...bne $at,$zero,3284

    ###########################################################
    # All bytes have been copied to the target space.  We still
    # need to:
    # - set up forwarding pointer in source object
    # - update the pointer in the current stack slot
    ###########################################################

    # set up the forwarding pointer
3308#    sw $zero,-8($t1) # 0 in first header-slot
3312#    sw $t8,-4($t1) # forwarding pointer in second header-slot

    # update the heap-slot with the address in the target space
3316#    sw $t8,($t0)

3320:  gcGoNextPointerSlot:
    # bump $t0 to next slot in object; if not done, loop back
3320#    addu $t0,$t0,4
3324#    bltu $t0,$s1,gcNextPointerInObject
3325##...sltu $at,$t0,$s1
3329##...bne $at,$zero,3168

3332:  gcTestIterDone2:
3332#    bltu $t0,$s3,gcHeapCopyLoop
3333##...sltu $at,$t0,$s3
3337##...bne $at,$zero,3052

    ###### end outer loop to copy all non-stack-direct objects ######

3340:  gcFinishUp:
    # restore $s2 to its updated value by popping off stack
3340#    lw $s2,($sp)
3344#    addu $sp,$sp,4

    # mark us as having already GC'd
3348#    move $t5,$s5

    # go try and allocate again
3352#    j gcJoin
        

########################################################
# vm_init (millicode)
#  - initialzes the virtual machine
#  - values 5 stored in $s5
#  - zero ("this pointer") stored in $s2
#  - heap and heap-limit pointers stored respectively in $s3 and $s4
#  - address of bottom of stack stored in 'stack_bottom' memory
#    location
#  - (note: 'cur_seg' memory location is already set to 0)
########################################################
3356:vm_init:

    # mark bottom of stack
3356#    sw $sp,stack_bottom
    
    # set "this" pointer to be null, for now
3360#    move $s2,$zero

    #set up the "five" register
3364#    li $s5,5

3368#    la $s3,seg0_start
3372#    la $s4,seg0_end
3373##...lui $s4,16
3377##...addiu $s4,$s4,4212
    
    # return
3380#    jr $ra

########################################################
# divide (millicode)
#  - divides first arg by second (signed divide)
#  - aborts if divisor is zero
#  - follows calling conventions for library methods
#  - parameters:
#    - ($sp) divisor
#    - 4($sp) (dummy) GC tag
#    - 8($sp) dividend
#    - 12($sp) (dummy) GC tag
#  - return value:
#    - ($sp) result
#    - 4($sp) (dummy) GC tag
########################################################
3384:divide:
    # get parameters; abort if divisor zero
3384#    lw $t0,($sp)
3388#    lw $t1,8($sp)
3392#    beq $t0,$zero,divByZeroError
    
    # perform division
3396#    div $t1,$t0
3400#    mflo $t0

    # store result, adjust stack and return
3404#    addu $sp,$sp,8 # adjust stack
3408#    sw $t0,($sp) # store result
3412#    jr $ra

########################################################
# remainder (millicode)
#  - takes remainder first arg divided by second (signed divide)
#  - aborts if divisor is zero
#  - follows calling conventions for library methods
#  - parameters:
#    - ($sp) divisor
#    - 4($sp) (dummy) GC tag
#    - 8($sp) dividend
#    - 12($sp) (dummy) GC tag
#  - return value:
#    - ($sp) result
#    - 4($sp) (dummy) GC tag
########################################################
3416:remainder:
    # get parameters; abort if divisor zero
3416#    lw $t0,($sp)
3420#    lw $t1,8($sp)
3424#    beq $t0,$zero,divByZeroError
    
    # perform division
3428#    div $t1,$t0
3432#    mfhi $t0

    # store result, adjust stack and return
3436#    addu $sp,$sp,8 # adjust stack
3440#    sw $t0,($sp) # store result
3444#    jr $ra
    
########################################################
# checkCast (millicode) - checks that a cast is legal
#  - aborts if null
#  - aborts if cast is illegal cast
#  - parameters:
#    - ($sp) object to check
#    - $t0 address of vtable for target-class
#    - $t1 address one past vtable address of last
#      subclass of target-class
#  - return value:
#    - ($sp) object to check (now checked)
#  - side-effects: clobbers $t2 and $t3
########################################################
3448:checkCast:
    # get object, allow immediately if null
3448#    lw $t2,($sp)
3452#    beq $t2,$zero,checkCastReturn
    
    # get vtable address of object, abort if less than
    # lower limit or greater then or equal to higher
    # limit
3456#    lw $t2,-12($t2) # vtable address
3460#    bge $t2,$t1,castException
3461##...slt $at,$t2,$t1
3465##...beq $at,$zero,3636
3468#    blt $t2,$t0,castException
3469##...slt $at,$t2,$t0
3473##...bne $at,$zero,3636
    
    # return, leaving object unchanged on stack
3476:  checkCastReturn:    
3476#    jr $ra    

# checkCast:
#     # get object, allow immediately if null
#     lw $t2,($sp)
#     beq $t2,$zero,checkCastReturn
#     
#     # get vtable address of object (using Object vtable
#     # address for arrays)
#     lw $t3,-8($t2) # <= 0 if array
#     lw $t2,-12($t2) # vtable address (unless array)
#     bgt $t3,$zero,skipArrayCast
#     la $t2,CLASS_Object
#     
#     # get vtable address of object, abort if less than
#     # lower limit or greater then or equal to higher
#     # limit
#   skipArrayCast:
#     bge $t2,$t1,castException
#     blt $t2,$t0,castException
#     
#     # return, leaving object unchanged on stack
#   checkCastReturn:    
#     jr $ra    

########################################################
# instanceOf (millicode) - tests whether an object is
#   a member of a given class (or subclass)
#  - returns false if object is null
#  - parameters:
#    - ($sp) object to check
#    - $t0 address of vtable for target-class
#    - $t1 address one past vtable address of last
#      subclass of target-class
#  - return value:
#    - ($sp) true (1) or false (0), depending on whether
#      object is a member
#  - side-effects: clobbers $t2 and $t3
########################################################
3480:instanceOf:
    # get object, go return false if null
3480#    lw $t2,($sp)
3484#    beq $t2,$zero,doneInstanceOf
    
    # get vtable address of object, determine if we're >= the
    # lower limit, and if we're < the higher limit
3488#    lw $t2,-12($t2) # vtable address
3492#    sge $t0,$t2,$t0 # are we >= the lower limit?
3493##...slt $t0,$t2,$t0
3497##...xori $t0,$t0,0x1
3500#    slt $t1,$t2,$t1 # are we < the higher limit?
    
    # store the AND of the two conditions onto the stack; return
3504#    and $t2,$t0,$t1
3508#    sw $t2,($sp)
3512:doneInstanceOf: # if we reach here via branch, stack-top is zero,
            # which will now represent false
3512#    jr $ra


###########################################################
# jump-targets to terminate program:
# - exit: returns normally
# - outOfHeapMemory: prints "out of memory" error and returns
###########################################################

3516:exitError:
    # assumes $a0 has pointer to null-terminated string

    # print the string
3516#    li $v0,4 # syscall-code for print-string
3520#    syscall
3524:exit:
    # print termination message
3524#    li $v0,4 # syscall-code for print-string
3528#    la $a0,exitString
3532#    syscall

    # print number of garbage collections
3536#    li $v0,4 # syscall-code for print-string
3540#    la $a0,gcCountMsg
3544#    syscall # print "Number of ..."
3548#    lw $a0,gcCount # number of GC's
3552#    li $v0,1 # code for print-int
3556#    syscall
3560#    li $v0,4 # syscall-code for print-string
3564#    la $a0,newlineString
3568#    syscall # print newline

    # terminate execution
3572#    li $v0,10 #syscall-code for "exit"
3576#    syscall

3580:outOfHeapMemory:
3580#    la $a0,heapMemoryMsg
3584#    j exitError
3588:divByZeroError:
3588#    la $a0,divByZeroMsg
3592#    j exitError
3596:strIndexOutOfBounds:
3596#    la $a0,strIndexOutOfBoundsMsg
3600#    j exitError
3604:arrayIndexOutOfBounds:
3604#    la $a0,arrayIndexOutOfBoundsMsg
3608#    j exitError
3612:arraySizeOutOfBounds:
3612#    la $a0,arraySizeOutOfBoundsMsg
3616#    j exitError
3620:nullPtrException:
3620#    la $a0,nullPtrExceptionMsg
3624#    j exitError
3628:badIntegerFormat:
3628#    la $a0,badIntegerFormatMsg
3632#    j exitError
3636:castException:
3636#    la $a0,castExceptionMsg
3640#    j exitError
############## data section ################
    .data
3840:hexChars:
    .ascii "0123456789abcdef"
    .align 2
3856:cur_seg:
    .word 0

3860:lastCharRead:
    .word -2 # -2 => no buffered character
3864:prevHashConstant:
    .word 0x2785173c
3868:gcCount:
    .word 0

3872:heapMemoryMsg:
    .asciiz "ERROR: out of heap memory\n"
3899:divByZeroMsg:
    .asciiz "ERROR: divide by zero\n"
3922:strIndexOutOfBoundsMsg:
    .asciiz "ERROR: string index out of bounds\n"
3957:arrayIndexOutOfBoundsMsg:
    .asciiz "ERROR: array index out of bounds\n"
3991:arraySizeOutOfBoundsMsg:
    .asciiz "ERROR: array size out of bounds\n"
4024:nullPtrExceptionMsg:
    .asciiz "ERROR: null-pointer exception\n"
4055:badIntegerFormatMsg:
    .asciiz "ERROR: attempt to read badly formatted integer\n"
4103:castExceptionMsg:
    .asciiz "ERROR: illegal cast\n"
4124:gcMsg: 
      .asciiz "\nGC!\n"
4130:gcCountMsg: 
      .asciiz "Number of garbage collections: "

4162:nullStr:
    .asciiz "null"
4167:trueString:
    .asciiz "true"
4172:falseString:
    .asciiz "false"
4178:exitString:
    .ascii "Program execution completed."
    #### "falls through" here
4206:newlineString:
    .asciiz "\n"
    
    .align 2
4208:stack_bottom:
    .word 0

# heapFudge: # temporary fudge amount to exercise GC
#     .word 0

4212:seg0_start:
    .space 0x100000
1052788:seg0_end:
1052788:seg1_start:
    .space 0x100000
2101364:seg1_end:
